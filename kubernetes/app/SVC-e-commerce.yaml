apiVersion: v1
kind: Service
metadata:
  name: easyshop-service
  namespace: tws-e-commerce-app
  labels:
    app: easyshop
spec:
  type: NodePort
  selector:
    app: easyshop
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30080
      protocol: TCP